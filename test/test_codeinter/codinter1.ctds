/* Caso de prueba */
boolean a,b,c,d,e;
boolean f;
int g,h,i,j;
int k;

int incr(int x){
    while(x>1){
        x = x + 1;
        while(x > 10){
            x = x + 1;
        }
    }
    return x + 1;
}

boolean hastaIgual(int x, int z){
    boolean m;
    while(!(x == 10)){
        if (x>10){
            x = x - 1;
        }else{
            if(x<10){
                x = x + 1;
            }
        }
    }
    return m;
}

void main(){
    k = incr(k);
    a = hastaIgual(g,h);
    return ;
}
